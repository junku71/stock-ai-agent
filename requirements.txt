# 서버(FastAPI + 국내주식 스케줄러) 실행에 필요한 패키지만 둔다.
# ML 학습/예측은 Kaggle 커널(kaggle_notebook_kr/predict_kr.py)에서 돌아가며
# tensorflow/scikit-learn 은 Kaggle 이미지에 이미 들어 있으므로 여기 넣지 않는다.

fastapi>=0.100.0
uvicorn>=0.23.0
supabase>=2.0.0
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
pytz>=2023.3
schedule>=1.2.0
anthropic>=0.30.0      # 뉴스 감성 스코어링 / LLM 매수·매도 검토 / 분석 리포트
reportlab>=4.0.0       # 분석 리포트 PDF 생성
kaggle>=1.6.0          # ML 예측 노트북 원격 트리거
openpyxl>=3.1.0        # 1단계 퀀트 스크리닝 xlsx 파싱 (kr_quant_data_service)
